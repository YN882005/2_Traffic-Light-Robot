## Name: Youssef Nasser Farouk Elsayed Mohamed Kandiel

# Project(2): Traffic Light Robot

# How to such project:
State machine: Control state transitions in nextState() to cycle strictly from Green to Yellow to Red to Green.
Bitwise flags: Manipulate status modes (NIGHT, BUSY, BLINK_ON) using custom shift macros (SET_BIT, CLR_BIT, TOGGLE_BIT, READ_BIT).
Log buffer: Implement a rolling buffer in pushLog() that shifts the 20-character history array left whenever a new letter is appended.

# Answer to why question: 
* I couldn't find any why question to answer.